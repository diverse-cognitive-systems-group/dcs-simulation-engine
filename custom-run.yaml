run:
  name: ii4938

  provider:
    # location: local
    location: fly
    config:
      host: 0.0.0.0
      port: 8080
  
  interfaces: ["gui", "api"]

games:
  - name: Infer Intent
    overrides:
      banner: "<b>A TEST – PT</b>"
