name: Explore
version: 1.0.0
authors: ["DCS"]
description: |
  Players are given no tasks -- an open-ended playground.

stopping_conditions:
  runtime_seconds: [">=600"]
  turns: [">=50"]

game_class: dcs_simulation_engine.games.explore.ExploreGame
