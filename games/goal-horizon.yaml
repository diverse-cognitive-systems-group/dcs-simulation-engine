name: Goal Horizon
version: 1.0.0
authors: ["DCS"]
description: |
  Players are tasked with understanding the capabilities and limitations of another character.

stopping_conditions:
  runtime_seconds: [">=3600"]
  turns: [">=500"]

game_class: dcs_simulation_engine.games.goal_horizon.GoalHorizonGame
