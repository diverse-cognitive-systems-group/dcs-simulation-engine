# DCS Simulation UI

A browser-based interface for the DCS (Decision and Communication Simulation) engine. Registration-required runs ask players to register with a one-time access key, browse available simulation games, configure a session by selecting characters, and then interact with an AI-driven scenario in real time over a WebSocket connection. No-registration runs issue anonymous player keys automatically and skip the registration screen.

## Prerequisites

- [Bun](https://bun.sh) — used as the package manager and script runner
- The DCS API server running on port 8000 — see `dcs_simulation_engine/api/`

The Vite dev server proxies HTTP `/api/` requests to `http://localhost:8000`, so the API server must be running before you start the UI.
Live gameplay WebSockets connect directly to the backend on port `8000` in development so chat does not depend on Vite's WebSocket proxy implementation.

For anonymous local play, start the backend with `uv run dcs server --config examples/run_configs/demo.yml`.

## Getting Started

```bash
# Install dependencies
bun install

# Start the development server
bun run dev
```

If your API server is not running on the same host as the browser, set `VITE_API_ORIGIN` before starting the UI:

```bash
VITE_API_ORIGIN=http://your-api-host:8000 bun run dev
```

Then open [http://localhost:5173](http://localhost:5173) in your browser.

## Docker Image

The Docker UI container is built from [`docker/ui.dockerfile`](../docker/ui.dockerfile).
It runs the same Bun/Vite workflow as local development from the `ui/` folder.
In Docker Compose, Vite proxies HTTP `/api/*` requests to the `api` service via
`VITE_API_PROXY_TARGET=http://api:8000`.

## Available Scripts

| Command | What it does |
|---|---|
| `bun run dev` | Start the Vite dev server with hot module replacement |
| `bun run build` | Type-check with `tsc` then bundle for production into `dist/` |
| `bun run preview` | Serve the production build locally for a final check |
| `bun run lint` | Run Biome to check for lint errors and formatting issues |
| `bun run format` | Run Biome to auto-format all source files |
| `bun run codegen` | Regenerate typed API hooks from the backend's OpenAPI schema (see below) |

## Tech Stack

| Tool | Purpose | Docs |
|---|---|---|
| Vite | Dev server and bundler | https://vite.dev |
| React | UI component model | https://react.dev |
| TypeScript | Static typing | https://typescriptlang.org |
| TanStack Router | Type-safe client-side routing | https://tanstack.com/router |
| TanStack Query | Async data fetching and caching | https://tanstack.com/query |
| React Hook Form | Form state management | https://react-hook-form.com |
| Zod | Runtime schema validation | https://zod.dev |
| Tailwind CSS | Utility-first styling | https://tailwindcss.com |
| shadcn/ui | Pre-built accessible component primitives | https://ui.shadcn.com |
| Biome | Linter and formatter (replaces ESLint + Prettier) | https://biomejs.dev |
| Orval | Generates typed React Query hooks from OpenAPI | https://orval.dev |
| Bun | JavaScript runtime and package manager | https://bun.sh |

## Project Structure

```
src/
├── main.tsx                  # Entry point: mounts React, sets up Router and QueryClient
├── index.css                 # Global styles and Tailwind base imports
│
├── routes/
│   ├── routeTree.ts          # Assembles all routes into the tree passed to createRouter
│   ├── __root.tsx            # Root route (renders <Outlet />) and requireAuth guard
│   ├── index.tsx             # "/" — redirects based on auth state
│   ├── login.tsx             # "/login" — access-key login form
│   ├── signup.tsx            # "/signup" — participant registration form
│   ├── games/
│   │   ├── index.tsx         # "/games" — grid of available games
│   │   └── $gameName.tsx     # "/games/:gameName" — character selection and session creation
│   └── play/
│       └── $sessionId.tsx    # "/play/:sessionId" — live chat UI connected via WebSocket
│
├── components/
│   ├── chat-message.tsx      # ChatMessageBubble: renders one message with role/event styling
│   └── ui/                   # shadcn/ui primitives (Button, Card, Input, etc.) — do not edit
│
├── hooks/
│   └── use-session-websocket.ts  # Manages WebSocket lifecycle, auth handshake, and message state
│
├── api/
│   ├── http.ts               # httpClient: fetch wrapper that injects the Bearer token
│   └── generated/            # Auto-generated by Orval — do not edit by hand
│
└── lib/
    ├── auth.ts               # sessionStorage helpers for the access key and player info
    └── utils.ts              # cn() — merges Tailwind class names
```

## API Code Generation (Orval)

Orval generates typed models and React Query hooks into `src/api/generated/` from OpenAPI.

### Option A: Regenerate from a running API server

1. Start the API server so `http://localhost:8000/openapi.json` is available.
2. From `ui/`, run:

```bash
bun run codegen
```

### Option B: Regenerate from an OpenAPI snapshot (recommended for deterministic local/CI runs)

1. From repo root, generate a snapshot:

```bash
uv run python -c "import json; from unittest.mock import MagicMock; from dcs_simulation_engine.api.app import create_app; print(json.dumps(create_app(provider=MagicMock()).openapi()))" > ui/openapi.snapshot.json
```

2. From `ui/`, point Orval at the snapshot:

```bash
ORVAL_OPENAPI_TARGET=./openapi.snapshot.json bun run codegen
```

### Verify after codegen

1. Review generated diffs in `src/api/generated/`.
2. Run:

```bash
bun run lint
bun run build
```

The generated files are committed to the repository. Do not edit them by hand; any manual changes will be overwritten on the next `codegen` run.

## Authentication

Authentication uses a bearer access key issued at registration when the server runs in standard mode. The key is stored in `sessionStorage`, which is scoped to the current browser tab and cleared automatically when the tab closes. There are no refresh tokens or persistent sessions; closing the tab requires signing in again.

For authenticated app API calls, the key is sent as an `Authorization: Bearer <key>` header (via `src/api/http.ts`). Login (`/api/player/auth`) and registration (`/api/player/registration`) are unauthenticated bootstrap requests. The key is also sent as an `auth` WebSocket frame at the start of each game session (via `src/hooks/use-session-websocket.ts`).

When the active run config has `ui.registration_required: false`, the UI discovers that from `/api/server/config`, creates an anonymous player key, and starts gameplay without showing registration.
